{
	"name": "arcadaeum",
	"private": true,
	"scripts": {
		"dev": "docker compose up",
		"dev:detached": "docker compose up --build -d",
		"reset": "docker compose down -v",
		"logs": "docker compose logs -f",
		"urls": "node -e \"console.log('\\n🎮 Dev URLs:\\nFrontend: http://localhost:5173\\nBackend: http://localhost:8000/health\\nPostgres: localhost:5432\\n\\n')\"",
		"check:users": "docker compose exec db psql -U app_user -d app_db -c 'SELECT id, username, email, display_name, oauth_provider, oauth_id FROM users;'",
		"setup:py": "cd backend && ([ -d .venv ] || python3 -m venv .venv) && (./.venv/bin/pip install -r requirements.txt -r requirements-dev.txt || .\\.venv\\Scripts\\pip install -r requirements.txt -r requirements-dev.txt)",
		"setup:py:win": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"cd backend; if (!(Test-Path .venv)) { py -m venv .venv }; .\\.venv\\Scripts\\python -m pip install -r requirements.txt -r requirements-dev.txt\"",
		"start:win": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"npm run setup:py:win; npm run dev; npm run urls\"",
		"start": "npm run setup:py && npm run dev && npm run urls",
		"stop": "docker compose down"
	}
}
